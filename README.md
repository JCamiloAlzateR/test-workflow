# Test Workflow

Small project to test the 3-layer review gate.

## Structure

- `src/` — Python source
- `tests/` — pytest tests
- `.pre-commit-config.yaml` — hooks
- `.github/workflows/review-gate.yml` — CI pipeline
